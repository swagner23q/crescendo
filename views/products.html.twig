<!DOCTYPE html>
<html>
    <head>

    </head>
    <body>
        {% for product in products %}
            <img src='{{ product.getImg }}'>
            <p>{{ product.getName }}</p>
            <p>{{ product.getPrice }}</p>        
        {% endfor %}
    </body>
</html>
